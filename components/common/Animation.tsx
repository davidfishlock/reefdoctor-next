import { Box, Center, Flex, HStack, SimpleGrid } from '@chakra-ui/react'
import { motion } from 'framer-motion'

export const AnimatedBox = motion(Box)
export const AnimatedFlex = motion(Flex)
export const AnimatedCenter = motion(Center)
export const AnimatedHStack = motion(HStack)
export const AnimatedSimpleGrid = motion(SimpleGrid)
